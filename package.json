{
  "name": "solring-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension showing metrics and useful information for evaluating decks on Moxfield.",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs"
  }
}
